# The following variablese are set here and need to be sourced
# from both the installer script and the zsh configuration files
# in order to share the same values.
#
# When you run the installation script you cannot consider zsh
# configured, hence it cannot inherit these variables.

_zsh_cache_dir=~/.cache/zsh
_tmux_dir=~/.config/tmux
_xdg_config_home=~/.config
_less_cache_dir=~/.cache/less
_pyenv_installation_dir=~/.config/pyenv/pyenv
_pyenv_root_dir=~/.config/pyenv
_pyenv_virtualenv_cache_dir=~/.cache/pyenv-virtualenv
