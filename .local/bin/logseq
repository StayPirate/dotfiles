#!/bin/sh
exec /usr/bin/flatpak run --branch=stable --arch=x86_64 com.logseq.Logseq --ozone-platform-hint=auto --enable-features=WaylandWindowDecorations --enable-features=WebRTCPipeWireCapturer "$@"
